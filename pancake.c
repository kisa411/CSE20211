#include <stdio.>

int flip(int i)

int main() {
	int i; 

	printf("Please put in the stack values:");
	scanf("%d", &stack[]); //put or read user input into array

	//call function
	flip(i);

	return 0;
}

int flip(int i) {

	//compare and contrast the values and see if they're in sequential order
	for(i = 0; i < sizeof(stack)/sizeof(int); i++) {
		if (stack[i] == stack[i]-1) {
			continue
		} else { 
			//need to reverse numbers (flip!)
			reverse = //assign reerse to reversed values
		}
	}

	return reverse;

}